{
  "name": "machineassist-root",
  "private": true,
  "version": "1.0.0",
  "description": "Industrial Diagnostics Copilot Root Workspace",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
